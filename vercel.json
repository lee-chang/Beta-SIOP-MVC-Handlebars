{
   "version": 2,
   "installCommand": "npm install",
   "buildCommand": "npm run build",
   "builds": [
       { "src": "src/app.js", "use": "@vercel/node" }
    ],
    "routes": [
       { "src": "/(.*)", "dest": "src/app.js" }
    ]
}